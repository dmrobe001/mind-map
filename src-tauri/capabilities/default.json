{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Everything the UI window is allowed to reach for. Commands defined by this app are always callable; only plugin commands need listing here, and the app asks for the narrowest set that works.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default"
  ]
}
